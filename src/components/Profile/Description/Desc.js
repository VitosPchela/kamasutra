import React from 'react';
import ss from "./Desc.module.css";

const Desc = () => {
    return (
        <div className={ss.main}>
            Some text about profile
        </div>
    );
}

export default Desc;